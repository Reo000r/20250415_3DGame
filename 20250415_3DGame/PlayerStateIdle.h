#pragma once
#include "PlayerState.h"

/*
class PlayerStateIdle final : public PlayerState {
public:
    PlayerStateIdle();
    ~PlayerStateIdle();

    void Enter(std::shared_ptr<Player> player) override;
    void Update(std::shared_ptr<Player> player) override;
    void Exit(std::shared_ptr<Player> player) override;

    /// <summary>
    /// 状態が変化するかどうかのチェック
    /// </summary>
    void CheckTransition(std::shared_ptr<Player> player) override;
};
*/
